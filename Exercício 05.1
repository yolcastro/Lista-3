//Com strcat

#include <stdio.h>
#include <string.h>

#define TAM 100

int main() {
    char str1[TAM], str2[TAM];

    puts("String 1:");
    fgets(str1, TAM, stdin);
    str1[strcspn(str1, "\n")] = '\0'; 

    puts("String 2:");
    fgets(str2, TAM, stdin);
    str2[strcspn(str2, "\n")] = '\0';

    strcat(str1, str2);

    printf("Resultado: %s\n", str1);

    return 0;
}
